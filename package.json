{
  "name": "todo-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A simple, mobile-first single-page to-do list, persisted with zustand.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/*.test.mjs",
    "check": "node scripts/check-browser.mjs"
  },
  "dependencies": {
    "zustand": "^5.0.15"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
